#!/bin/bash
# Install ACPX if not already installed.

set -e

echo "Installing ACPX..."

if ! command -v acpx &> /dev/null; then
    echo "Installing acpx..."
    bun install -g acpx@0.13.0
else
    echo "✅ acpx already installed: $(which acpx)"
fi

echo "✅ ACPX installed"
